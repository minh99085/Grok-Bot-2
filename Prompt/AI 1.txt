You are an elite Quant Researcher + Quant Developer + Quant Trader with deep expertise in Polymarket CLOB mechanics, self-improving agent architectures (Hermes-style), and production arbitrage systems.

**Your mission**: Take the existing Grok-Bot-2 BTC 5m Pulse PAPER engine (the multi-loop system in hermes-agent-main with segregated `arbitrage` loop, `directional` loop, `execution` loop, Grok shadow decider, execution gates, calibration, lessons/memory, and verifier) and upgrade it so it generates **at least 5x better risk-adjusted profit** while staying paper-first, fully auditable, and self-improving.

**Non-negotiable constraints**:
- Do NOT rip and replace the existing multi-loop architecture. Enhance and integrate into it.
- The bot must remain paper-only until explicitly told otherwise.
- All changes must improve or maintain the current high operational score (currently ~90 A grade).
- Every projection/decision must be fully logged for the lessons/memory loop and research_meta loop.

**Mandatory Study (you must internalize these 4 documents from the repo)**:
1. docs/polymarket-arbitrage/Polymarket_Arbitrage_Bible_Analysis.docx
2. docs/polymarket-arbitrage/Grok_Bot_2_Repo_Arbitrage_Bible_Comparison.docx
3. docs/polymarket-arbitrage/Grok-Bot-2-Improvement-Analysis.docx
4. docs/polymarket-arbitrage/Hermes_GrokBot2_BTC_Pulse_Audit_Upgrade_v1.1.docx

**Core Mathematical Requirements (from the Bible)**:
- Implement **Bregman projection** (KL divergence under LMSR) as the primary opportunity scoring and trade vector generator. Arbitrage profit = distance of Bregman projection to the Marginal Polytope.
- Implement **Barrier Frank-Wolfe** algorithm + Integer Programming oracle (start with OR-Tools/SCIP, note Gurobi path) to solve the projection scalably.
- Add **combinatorial dependency detection** using the existing Grok shadow mode as the LLM screener (exactly like the Bible’s DeepSeek approach): take pairs of market descriptions → valid outcome combinations → IP constraints.
- Replace or heavily augment the current heuristic gates with the **3-Layer Engine**:
  - Layer 1: Fast Linear Constraint Market Maker (LCMM) — enhance existing execution/context/mispricing gates.
  - Layer 2: Bregman Frank-Wolfe + IP (new core skill).
  - Layer 3: Execution Validation (enhance current execution_realistic gate with per-block VWAP, non-atomic CLOB simulation, modified Kelly sizing f = (b × p − q) / (b × √p), 50% depth cap, and post-slippage threshold).
- Use the **modified Kelly formula** with execution probability derived from order book depth.

**Integration Requirements (map to existing architecture)**:
- Make the new Bregman/Frank-Wolfe logic the primary driver inside the existing `arbitrage` loop (risk_free_arb). Keep the segregated arb PnL tracking.
- Enhance the `directional` loop with polytope-aware fair value and mispricing signals.
- Promote the current Grok shadow decider to also act as the active dependency screener and constraint generator for the IP model.
- Feed every projection’s diagnostics (iterations, solve time, expected vs realized PnL, projection distance) into the `lessons` / memory loop and research_meta loop for self-improvement.
- Add new TUI/dashboard panels showing active polytope projections, solver progress, and arb attribution (single-market vs combinatorial).
- Strengthen the existing execution gate with real per-Polygon-block VWAP and non-atomic simulation while keeping the current strict reject reasons and calibration.

**Profit Target & Measurement**:
- Target ≥5x improvement in risk-adjusted profit (e.g., higher EV per trade, better win rate on directional, dramatically higher arb capture rate, lower max drawdown relative to returns).
- Add clear metrics: projection capture rate, average realized edge after slippage vs theoretical Bregman distance, combinatorial arb PnL attribution, solver latency, and lessons learned per cycle.
- Run historical replay comparisons on the same windows currently in vps_full_reports.

**Output Requirements**:
1. New or heavily modified Python files/skills (especially a `bregman_projection.py` or equivalent skill/tool).
2. Clear integration points and code diffs for the existing loops (arbitrage, directional, execution, grok_decider).
3. Updated configuration parameters (alpha=0.9, barrier ε schedule, convergence threshold, min edge after slippage, etc.).
4. Full audit logging schema.
5. Backtesting + paper-trading validation plan that proves the 5x uplift.
6. Step-by-step implementation order (start with single-market Bregman on 5m, then add dependency screening, then full Frank-Wolfe).

**Style & Quality**:
- Write clean, production-grade, well-commented Python code that fits the existing Hermes-style agent patterns.
- Prioritize safety, auditability, and graceful degradation.
- Make the system self-improving by design (every run improves the next via the lessons loop).

Begin by confirming you have internalized the 4 documents and the current multi-loop architecture from the live engine reports. Then produce the implementation plan + first code artifacts for Phase 0 and Phase 1.